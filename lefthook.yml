commit-msg:
  commands:
    commitlint:
      run: npx commitlint --edit {1}
pre-commit:
  commands:
    lint:
      run: npx eslint {staged_files}
    format:
      run: npx prettier --check {staged_files}
    test:
      run: npm run test
